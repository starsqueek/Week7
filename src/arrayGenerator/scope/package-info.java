/**
 * Defines scopes and provides some implementations thereof.
 *
 * A scope defines a set of values.  A scope can be used to provide a set of acceptable values for an
 * {@link arrayGenerator.generator.ArrayGenerator}.
 */
package arrayGenerator.scope;